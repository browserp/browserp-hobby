# Launch follow-up — 4 September 2026

Follow-up to release `b8ab337`. This record preserves the full scope while the website release and editorial selection proceed in parallel.

- [x] Diagnose the homepage advert artwork: real Chrome rejected all three files with `ERR_BLOCKED_BY_CLIENT`; the images remain available and decode correctly in the in-app browser. Implement compact, labelled fallback copy and retained navigation without changing blocker settings or requesting disguised alternate URLs. Healthy slides recover their normal layout.
- [x] Implement live refresh health on staff Overview and Scrapers. It reads real scheduled-run telemetry and source freshness, with last success, current/stale totals, failures and history. Permission/MFA checks apply; no scheduler secrets, private addresses or raw errors are exposed. Read-only migration `20260904112122_staff_refresh_health` applied.
- [x] Review all 55 existing communities for editorial quality as well as functional health. See the separate FiveM and RedM/Minecraft matrices. This review does not certify friendliness or actual staff enforcement; uncertain cases are explicitly identified.
- [x] Record four reversible launch holds through the authenticated staff panel: Lucid Heights, West Coast, PeakRP and Blaine County Network. Reasons concern founding/rebuilding recruitment and insufficient evidence of regularly attended sessions for the established-community brief. No misconduct finding, account deletion or loss of listing history.
- [x] Publish researched Everfall through the FiveM scraper. Public page verifies English / Qbox in the correct metadata order, separate Discord and Cfx links, decoded official logo and current 15/315 source observation at 11:42 UTC. Region remains Not confirmed; unsupported banner omitted and optional paid content disclosed. The automatic refresh subsequently advanced the observation timestamp.
- [x] Align the staff research shortlist: the four held communities are excluded and Everfall appears exactly once. Initial updated live roster: 29 FiveM, 20 RedM, 3 Minecraft (52 total).
- [x] Run Chromium 151, Firefox 153 and WebKit 26.5 checks at 390/1440 widths. All 54 page layout checks passed, with no uncaught JavaScript errors or overflow. Healthy and blocked artwork and synthetic staff health states passed. Six public views encountered API rate limiting and are not claimed as data-flow passes; see `BROWSER_VERIFICATION_2026-09-04.md`.
- [x] Run full repository verification: 338 application tests plus 35 database tests, all 373 passed; syntax checked 122 JavaScript files.
- [x] Publish the artwork/health release through PR29, production commit `2522bfe8d4ebe7c8d1c6e70c8f35dd11cd379748`. The preview and production reached READY. Real authenticated staff health showed scheduled checks and truthful source freshness; real Chrome showed compact “Artwork unavailable” copy where its content blocker rejects the advert files. The in-app browser continues to decode the original artwork.
- [x] Complete the comparative Minecraft and RedM review and a further ten-candidate FiveM review. Retain Lord of the Craft, MassiveCraft and Stoneworks with their different RP styles made explicit. Quality takes precedence over the approximate 40 FiveM / 20 RedM / 3 Minecraft targets; do not pad a count with unqualified communities.
- [x] Publish SACRP, Resilient Roleplay and The Tackroom through the actual staff importer, after both review and confirmation. Check canonical join links, separate community links and decoded static logos. SACRP is Public; Resilient and Tackroom require applications. Tackroom is explicitly an equestrian sandbox with optional roleplay. Historical peaks are never copied into live player counts.
- [x] Record six additional reversible FiveM activity holds: Distinct, Ignite, Logic, Moonlit Hills, OzzyGaming DarkSide and San Andreas First Response. Their recorded regional evening observations remained below the requested approximately35 benchmark. These are bounded observation windows, not permanent “dead server” or misconduct findings.
- [x] Hold Lemoyne using a separate full-week history with peak23 and no35+ sessions. Correct Gilded's application information to its official first-login in-game quiz and classify it as allowlisted. CaliRP remains Not confirmed where the general admission evidence conflicts. Redline remains deferred per the user's latest instruction.
- [x] Add visible Public / Whitelisted / Not confirmed joining filters to Discover and game pages, preserving old URLs and raw listing metadata. SQL normalization migration `20260904124607_public_whitelisted_discovery_filters` is applied. Public facet counts remain hidden.
- [x] Recover automatically from stale directory/search results: refresh visible pages every minute, preserve filters/pagination/focus, respect rate-limit delays, expire old imported counts and retain usable results during transient failure. The original Cali screenshot's transient source cause was not reproduced; the missing UI recovery path was reproduced and repaired.
- [x] Fix the public menu's position, repainting and keyboard behavior. Close matches the opener exactly; pause only ambient background/brand animation while the modal is open. Escape and modal keyboard traversal work in Chromium, Firefox and WebKit, including filled search inputs. Normal brand and button motion resumes on close; reduced motion stays respected.
- [x] Run final repository verification after the code freeze: **351 application +35 database =386 passing tests**, no failures or skips, syntax checked123 JavaScript files. Independent filter/refresh review found no blockers and checked SQL parity, Retry-After,120-card bounded refresh, timeouts and focus retention.
- [x] Complete12 joining-filter browser cases and9 final native navigation configurations across Chromium151, Firefox153 and WebKit26.5. Slow-device/touch emulation, normal/reduced motion and controlled failure recovery are documented in [interaction evidence](launch-curation/public-interaction-performance-2026-09-04.md). These supplement the earlier54 page-shell checks; they are not physical-device certification.

## Continuation record

The database roster check at13:26UTC contains **48 published listings:25 FiveM,20 RedM and3 Minecraft**. All have import provenance. All49 stored image URLs returned200 image content; SAVRP intentionally has no logo because its generic source icon was rejected. This replaces the earlier52/55-listing snapshots above; those remain historical evidence.

Source availability is separate from editorial inclusion. At that snapshot44/48 observations were current. Subsequent targeted checks found newer responses for Badlands and Vital, an old True Grit heartbeat, and a404 for Ranch's known Cfx endpoint. Do not call every source current, invent a zero, or infer that a community has closed from a single failed observation. See the final roster review for follow-up evidence.

Remaining work after this release:

- Expand FiveM toward approximately40 only when additional candidates meet the same identity, community-management, gameplay and representative activity criteria. Nine researched alternatives are explicit holds; do not automatically import them.
- Revisit unavailable sources and borderline communities against later representative sessions. Preserve regional context, observations and reversible archive history. Do not duplicate/import an unproven replacement code.
- Keep Redline deferred; revisit only if the user requests it or reliable identity/status evidence becomes available. Roblox remains an applications-led later rollout.
- Complete a real Google/Discord sign-in and identity-linking flow with a consenting ordinary member and confirm the hosted manual-linking setting. Existing callback/CSRF/PKCE/state/nonce/ownership/staff restrictions and upload route tests pass, but these do not substitute for real provider consent. Do not alter the owner's staff identity to perform this check.
- Payments remain deliberately disabled pending financial testing. No payment readiness claim is made.
- Physical-device/GPU checks or field performance monitoring can supplement this release's browser-engine and emulation evidence. No claim of every browser version, device or screen reader is made.

The user's later messages add or refine scope; they do not cancel the other workstreams.

The user prefers to watch actual staff operations in the in-app browser. Use real Chrome only where a browser-specific check requires it. Do not claim that a browser-engine matrix covers every installed browser or device, that health proves community quality, or that operator advertising proves friendly staff.
