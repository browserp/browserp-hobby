# Moderation account-copy integration — 9 September 2026

Built and verified locally against moderation-integrated commit `732c3e13d31c3d015f8baaecf960cc215a31d425`. No hosted migration, provider configuration, deployment or real-account change was performed.

The existing structured collector omitted `private.content_submissions`. Migration `20260909104051_member_export_content_moderation.sql` preserves that collector and adds the current member's stored submissions under `collections.contentModeration`, including pending, published, blocked and superseded records, candidate content/source references, fingerprints, stored check outcomes, reasons and appeals. This exports the stored state; it does not invent historical decisions that the table does not retain. An explicit projection excludes private reviewer identities and internal provider fields. The approved-copy workflow, current-account/session binding, private grants and existing 2,000-record/10,000-total/byte limits remain in force. A larger copy fails visibly for follow-up instead of truncating.

Existing `uploads` metadata already covers all owned assets, including quarantined and rejected avatars. Existing private file delivery permits `uploads-quarantine` and checks the approved copy, current ownership, hash, size and object path. File delivery and receipts remain separate from the JSON copy; this change does not make quarantined content public or mark files received.

The existing erasure preflight discovers the new table through its actual foreign keys: owner/auth, owner/profile and comment dependencies cascade; asset and reviewer references become null. Its Storage inventory also covers owner/path/asset-linked objects and orphan quarantine objects. The regression loads the real moderation schema and preflight and verifies these dependencies without changing rows. This remains a count-only review with `executionEnabled=false` and `fullErasureInventory=false`. Database cascades do not remove Storage bytes; erasure execution and retention decisions are still separate unfinished work.

Validation: `node --test --test-concurrency=1 test/member-export-content-moderation-db.test.mjs test/member-export-duty-comments-db.test.mjs` passed 17 checks. These exercise real approved generation before and after the migration, preservation of earlier collections and digest/size metadata, private-profile owner access, cross-account denial, all three avatar moderation states, private function/table access, the real erasure dependency graph and oversized-copy rejection. No broad suite or hosted change was needed for this bounded fix.

Main must review and apply this additive migration after `20260909092918_guarded_content_moderation.sql`, then verify the exact hosted schema and approved-copy path before treating it as live.
