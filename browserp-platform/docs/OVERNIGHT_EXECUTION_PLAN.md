# BrowseRP completion plan — 6 September 2026

This is the execution plan for the existing launch scope. “Work mode” is the owner's nickname for continuing unattended, not a new app mode or permission boundary. Existing publication and configuration authorisation continues. Do not merge main, purchase services or send unsolicited outreach.

## Order and parallel ownership

1. Root owns the release branch, integration, live database/configuration changes, account access and final decisions. Review independently produced patches before application. Keep deployed commit, migration order and rollback boundary explicit.
2. Owner-flow agent completes reviewed owner changes and the contextual application follow-up. Fix actual catalog validation mismatches, preserve imported metadata and legacy tags, enforce permissions and keep source identity changes staff-reviewed.
3. SEO agent completes the free launch foundation: initial public content, correct status/canonicals, published-only sitemap, crawlable pagination, truthful branding/social metadata and unchanged interactive quality. Branded searches BrowseRP/Browse RP/browserp.com are a priority; monitor RPBrowse without renaming the brand or promising a ranking.
4. Roster agent closes remaining qualified 40 FiveM /20 RedM /3 Minecraft spaces using exact identity, current public rules/onboarding, source freshness and representative regional history. Do not fill a space with an unsupported claim. Root publishes through reviewed staff tools and checks actual public images, counts, links and facets.
5. When these bounded jobs finish, redirect freed capacity to still-open inherited flows: account linking/unlinking/recovery, uploads and claims, advertising/contact enquiries, privacy request fulfilment, security and recovery, branding, and the recorded public/staff design and animation issues. Preserve launch exclusions such as payments and unsupported games.
6. Research useful website/staff inspiration only after inherited gaps are closed. Make tangible improvements that fit BrowseRP, retain useful controls, and verify user benefit. Finish the design consistency pass across equivalent public/staff components without making the two interfaces identical.
7. Create the separate BrowseRP community Discord after the core website and reviewed launch roster. Keep channels purposeful, roles least-privileged, moderation/anti-raid protections strong, voice/music manageable, branding consistent and recovery owner-controlled. Do not install paid bots or allow bots unnecessary private/staff access.
8. Integrate, run the repository's complete required verification once on the final candidate, verify that exact hosted build and publish through the existing release process. Check production identity and essential flows afterwards. Report completed work and specific outstanding external actions honestly.

## Efficient verification and browser use

- Each agent runs focused meaningful tests on its bounded change, then hands back exact paths, evidence and remaining risks. Do not ask every agent to repeat the complete repository suite.
- Root performs full integrated gates after compatible patches are combined. Repeat tests only for new changes, failures or unresolved risks, with required gates preserved.
- Three-engine browser coverage is sequential and coordinated, with each owned context closed. Include mobile/desktop, narrow layout, keyboard, reduced motion and original-position menu close; reuse valid unchanged evidence. Emulation does not establish physical-device coverage.
- Use direct repository/provider tools where suitable. UI remains necessary for actual account configuration, staff workflows and interaction checks; stop presentation-only navigation and screenshots.
- Reuse a few account tabs, avoid duplicate research/browser sessions, and keep test worker concurrency at the measured efficient setting on the8GB M1. More simultaneous heavy processes are not assumed faster.
- Keep summaries compact and evidence durable. Do not re-explain the complete scope on each user message. Later steering adds to the existing plan unless explicitly replacing it.

## Blockers and owner involvement

If one item needs a sign-in, external verification or private recovery action, record the exact need and request it asynchronously. Continue every independent task before ending work. Never disable safeguards or invent success to work around a blocker. Do not read, print or store passwords, MFA setup secrets or recovery codes. Ask the owner to close code displays before any account-status inspection.

Access checked: GitHub and Vercel tools work; Supabase API works; Cloudflare and Google Cloud are signed in; Google Search Console domain ownership is verified; Bing Webmaster Tools is signed in; Discord is signed in as BrowseRP and its authenticator is enabled. Discord recovery-code replacement is explicitly deferred by the owner until tomorrow after an accidental displayed-code capture and an invalid-key error when attempting regeneration. The owner says the codes are saved. Do not claim they were rotated, repeat the replacement request tonight, inspect the codes, or disable the authenticator. The private settings display was closed by navigating back to Friends.

Google Search Console is processing its first data. Do not repeatedly poll or treat this as a whole-project blocker. Submit the repaired production sitemap once it is published, then leave collection to Google. Search rankings and external approval times are outside direct control.

The owner should keep the local Mac powered, connected, awake and the app running for this local execution. If a session ends, the repository continuity ledger and this plan preserve the next action; do not claim background execution without an active mechanism.

## Completion evidence

The final report must tie branch/commit, applied migrations, deployment URL and test evidence to the actual release. Include roster totals and unresolved quality holds, actual account/provider verification limits, recovery scope, free SEO setup, security settings, and any owner-only actions still pending. No unsupported “fully secure”, “perfect”, “best in the world” or guaranteed search-rank claims.
